# OpenCV 加载策略
# VITE_OPENCV_MODE:
#   "local"  - 始终从本地服务器加载，不走 CDN
#   "auto"   - 先尝试本地下载，超时后切 CDN（默认）
VITE_OPENCV_MODE=auto

# auto 模式下本地下载的最大等待时间（毫秒），仅计算网络下载耗时，不含 JS 解析和 WASM 初始化
VITE_OPENCV_DOWNLOAD_TIMEOUT=1000
