---
description: 禁止防御性 fallback 编码，异常应向前追溯根因而非就地吞掉
globs:
  - src/**/*.ts
  - src/**/*.vue
alwaysApply: true
---

# 禁止防御性 Fallback

## 核心原则

本项目的数据来源是**本地用户操作**，不是互联网不可信输入。数据在正常流程下**一定是合法的**，如果出现异常值，说明**上游逻辑有 bug**，应该修复根因而非在下游掩盖。

## 禁止行为

1. **禁止无脑 try-catch 吞异常**：不要 catch 后 `console.warn` 然后返回默认值，这会让 bug 变成静默数据错误
2. **禁止 `?? fallback` 掩盖 null**：如果某个值"不应该"是 null，不要用 `?? defaultValue` 绕过，应该追查为什么是 null
3. **禁止 `?.` 链式防御正常路径**：对于业务逻辑中"一定存在"的对象，不要用可选链，应该让它在 null 时立即报错以暴露问题
4. **禁止 `if (!data) return` 静默退出**：如果函数的前置条件是 data 必须存在，不要静默 return，应该 throw 或 console.error

## 允许的例外

- **用户输入校验**：表单输入、文件解析等确实可能非法的数据，可以做校验和提示
- **异步竞态**：组件卸载后回调触发、tab 切换后旧请求返回等时序问题，可以做 guard
- **外部 API 边界**：File System Access API、IndexedDB 等浏览器 API 调用，允许 try-catch 并做用户提示
- **初始化阶段**：响应式状态在数据加载前确实为 null 的阶段，模板中可用 `v-if` 守卫

## 正确做法

遇到运行时异常时：

1. 先看**调用栈**，找到第一个传出错误值的位置
2. 向上追溯：是哪一步把数据弄错了？是赋值遗漏？时序问题？还是类型不匹配？
3. 在**产生问题的源头**修复，而不是在**消费端**加防御
