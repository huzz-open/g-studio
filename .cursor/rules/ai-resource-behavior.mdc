---
description: AI behavioral rules when operating on workspace resources and .gs files
alwaysApply: true
---

# AI 资源操作行为规范

## .gs 文件操作

### 创建

使用 `createGsFile({ path, type, data })` 创建。type 必须是 `GsType` 枚举值之一。

### 保存

使用 `writeGsFile({ path, type, data, lastKnownVersion })` 写入。
- 保存前读取磁盘 version 做冲突检测
- 保存成功后更新本地 `gsLastKnownVersion`

### 从资源管理器打开

资源管理器读取 `.gs` 的 `type` 字段 → 通过 `getHandlerByGsType` 找到对应 handler → `router.push({ path: handler.route, query: { gs: filePath } })`

### 模块侧接收

`useTabRouteSync` 在 `onMounted` 中读取 `?gs=` query → 调用 `handleRouteIntent(gsPath)` → 清除 query

## 图片资源

图片文件不需要任何 sidecar 文件。`.gs` 文件通过相对路径 `./filename.png` 引用同目录的图片。

## 右键创建

在资源管理器对图片右键 → 选择模块 → 创建对应 type 的 `.gs` 文件，texture 字段引用该图片。

## 模块注册

每个模块在 `src/shared/module-registry/register-all.ts` 中注册 handler：
- `gsType`：对应的 GsType 枚举值
- `route`：模块路由路径
- `createLabel`：右键创建菜单中的显示文字
- `fileSummary`：资源管理器中的 .gs 文件摘要
